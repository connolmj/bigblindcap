export default function EquitiesPage() {
  return (
    <div style={{ padding: "36px 0 80px", maxWidth: 460 }}>
      <div
        style={{
          fontSize: 26,
          fontWeight: 600,
          letterSpacing: "-0.02em",
          lineHeight: 1.15,
          color: "var(--ink)",
          marginBottom: 10,
        }}
      >
        Write-ups and positions
      </div>
      <p style={{ margin: 0, fontSize: 14, lineHeight: 1.6, color: "var(--muted)" }}>
        Still being laid out — we’ll build this page next.
      </p>
    </div>
  );
}
